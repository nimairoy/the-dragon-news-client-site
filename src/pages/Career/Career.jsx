import React from 'react';
import useTitle from '../../hooks/useTitle';

const Career = () => {
    useTitle('Career')
    return (
        <div>
            this is career page
        </div>
    );
};

export default Career;